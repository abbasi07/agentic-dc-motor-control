================================================================================
PROJECT CONTINUITY BRIEF — Agentic Orchestration of DC Motor Control
================================================================================
Purpose: Hand this file to a new chat if the current conversation runs out of
context/tokens. It captures decisions, current state, and the planned sequence.

Last updated: 2026-07-23 (Streamlit chat-first UI wired to design agent + custom
motor + controller family pick; workstream E SaaS hardening is NEXT)
Repo: https://github.com/abbasi07/agentic-dc-motor-control
Local path: E:\Agentic AI Course\Projects\1_Agentic_Orchestration_of_DC_Motor_Control

--------------------------------------------------------------------------------
1. PROJECT GOAL (REVISED — WHY AGENTS / LLMs ARE JUSTIFIED)
--------------------------------------------------------------------------------
NOT the goal: sort two fixed PID gain sets with an LLM (that is a for-loop).

YES the goal: an **adaptive control-design agent** that, under changing natural-
language specs and plant uncertainty, iteratively chooses *what to try next*
(structure + parameters), evaluates with deterministic tools, and produces a
grounded design rationale — something a static script cannot decide well when
the failure mode and next method are open-ended.

Research framing (attractive + defensible):
  "LLM-orchestrated adaptive controller design for uncertain plants (DC motor
   digital twin), with deterministic evaluation, constraint-aware redesign, a
   simulation certification gate, and ablation vs non-agent baselines —
   software/simulation only (no hardware)."

DELIVERY SCOPE LOCK: **strictly software / simulation**. No MCU, no motor
driver, no physical deploy. "Deployment" means exporting a certified controller
artifact + report from the digital twin, not flashing hardware.

Original diagram still maps, but roles are reinterpreted:

  NL Specs + System Dynamics
            |
            v
  LLM Orchestrator (adaptive strategy — NOT a dumb sorter)
      | interprets specs -> formal constraints + scenario priorities
      | decides WHICH specialist agent to call next (and why)
      | on FAIL: diagnoses scorecard -> redesign action (gains vs structure vs ID)
            |
            v
  Specialist design agents (tools)
      | PID / Tuner Agent
      | MPC Agent           (Optimize Trajectory)
      | Robust Agent        (Maximize Stability under mismatch)
      | RL / Adaptive Agent (Adaptive Learning)
      | Plant ID Agent      (optional: propose identification experiments in sim)
            |
            v
  Deterministic Simulation & Evaluation Node (code ONLY)
            |
            v
  Scorecard JSON (pass/fail + metrics) + Design Rationale (LLM, grounded)
            |
            v
  Simulation Certification Gate (release artifact if and only if tests pass)
            |
            v
  Export package: controller JSON/code + scorecard + rationale PDF/MD
  (NO hardware tool)

HARD RULE: Tools compute metrics and pass/fail. LLM never invents numbers from
plots. LLM plans, diagnoses, explains, and chooses next *action* under uncertainty.

--------------------------------------------------------------------------------
2. WHAT FULLY JUSTIFIES LLM ORCHESTRATION (FEATURE PILLARS)
--------------------------------------------------------------------------------
These five pillars are REQUIRED for the research story. Ranking fixed candidates
alone does NOT justify an LLM.

P1 — Spec-to-Design Loop (NL -> formal requirements)
  Input: natural language performance/safety specs, e.g.
    "Settle under 1.2 s, overshoot < 8%, survive a small load (~0.01 N·m) after 1.5 s,
     keep |u| <= 12 V, prefer low effort if multiple pass."
  LLM output (structured JSON schema):
    - hard constraints
    - soft preferences / weights
    - scenario priorities (which tests matter most for THIS request)
  Code validates schema; rejects invalid / unsafe limits.
  Why LLM: specs are open-ended English; mapping to constraint sets is not a
  fixed enum early in the project and changes with user intent.

P2 — Uncertainty / Digital-Twin Stress Suite
  Plant is never assumed perfect. Always evaluate under:
    - nominal step
    - load disturbance
    - parameter mismatch (J, b, R scales)
    - measurement noise
    - (later) combined stress / worst-case batch
  LLM role: given which scenarios FAILED, decide whether to
    - retune same topology,
    - switch topology (PID -> robust / MPC),
    - or request plant identification / mismatch expansion.
  Why LLM: next structural choice depends on *failure pattern*, not one scalar.

P3 — Constraint-Aware Redesign (adaptive loop, not one-shot ranking)
  On fail, orchestrator must output a RedesignPlan, e.g.:
    - "tighten Ki / reduce Kd" (parameter tweak via tuner)
    - "switch to Robust agent — mismatch scenario failed"
    - "switch to MPC — saturation + constraint violation on voltage"
    - "run ID experiment — model trust too low"
  Loop: design -> evaluate -> diagnose -> redesign until pass or budget exhausted.
  Why LLM: branching redesign policy is combinatorial; fixed if/else becomes
  brittle as agents and failure modes grow. Ablation compares LLM policy vs
  hand-coded heuristic policy.

P4 — Simulation Certification / Release Gate (NOT hardware)
  System REFUSES to export a "certified" controller package unless:
    - all hard constraints pass on required scenarios, AND
    - (optional) worst-case / robustness batch OK.
  LLM role: explain BLOCK vs ALLOW using scorecard evidence; propose extra
  sim experiments or softer specs if blocked.
  Export artifact (SaaS-ready): gains/policy + scorecard JSON + rationale MD.
  Why LLM: human-facing gatekeeping + spec negotiation; code enforces the gate.
  Explicit non-goal: physical deployment / MCU flashing.

P5 — Ablation Study (scientific honesty)
  Always compare three pipelines on the SAME NL-spec test suite:
    A) Fixed script: enumerate controllers, evaluate, lexicographic/argmin
    B) Heuristic orchestrator: hand-coded if/else redesign rules (no LLM)
    C) LLM orchestrator: adaptive redesign + rationale
  Report: success rate, iterations to pass, cost (tokens/time), rationale quality.
  Why this matters: proves WHEN the agent helps vs when a for-loop wins.

--------------------------------------------------------------------------------
3. LOCKED DESIGN DECISIONS
--------------------------------------------------------------------------------
- Control objective: angular SPEED (omega), not position (position = later lab).
- Plant fidelity: full armature-controlled DC motor (electrical + mechanical).
- Early-to-final labs: **simulation only** — no hardware deployment in scope.
- First controller family: PID; advanced topologies later, SAME interface.
- Environment: uv-managed virtualenv (.venv); Python >= 3.12.
- LLM: OpenAI API via openai + python-dotenv; key in .env (gitignored).
- Demos: `examples/lab_01_*.py` … `examples/lab_08_*.py` (no Jupyter notebooks).
- GitHub: origin -> https://github.com/abbasi07/agentic-dc-motor-control.git (main)
- Controller interface (ALL topologies):
    reset()
    step(measurement, reference, dt) -> u
- Evaluation is deterministic and shared (dc_motor.evaluate_controller).
- Scalar score in lab_02 / evaluate is a DEMO aggregate (not a universal standard);
  primary selection is constraints-first, then preferences from the Spec Agent.

Plant parameters (CTMS — University of Michigan DC Motor Speed example):
  J = 0.01 kg·m^2
  b = 0.1 N·m·s/rad
  K = 0.01  (Kt = Ke in SI)
  R = 1.0 ohm
  L = 0.5 H
  Source:
  https://ctms.engin.umich.edu/CTMS/index.php?example=MotorSpeed&section=SystemModeling

Plant equations:
  L * di/dt + R * i = V - Ke * omega
  J * d(omega)/dt + b * omega = Kt * i

Transfer function (speed from voltage):
  G(s) = omega(s)/V(s) = K / [(Js+b)(Ls+R) + K^2]

--------------------------------------------------------------------------------
4. CURRENT STATE (DONE)
--------------------------------------------------------------------------------
[x] Plant + open-loop + closed-loop PID + metrics          (examples/lab_01_*)
[x] Project tooling (uv, git, GitHub, .env.example, README)
[x] Shared evaluation harness / scorecard JSON             (dc_motor/ + examples/lab_02_*)
[x] Spec Interpreter Agent (NL -> constraints JSON)                 (P1 DONE)
[x] PID Tuner / redesign tools under constraints                    (P3 tools DONE)
[x] Adaptive LLM Orchestrator loop (diagnose -> next action)        (P2+P3 DONE)
[x] Uncertainty stress suite expansion + failure tagging            (P2 DONE)
[x] Advanced topology agents (MPC / Robust / Adaptive / ID)         (P2/P3 DONE)
[x] Simulation certification gate + export package (no hardware)    (P4 DONE)
[x] Ablation: Fixed script vs Heuristic vs LLM orchestrator         (P5 DONE)
[x] Design-rationale report generator (grounded in scorecard)
[x] Professional package layout: examples/ + tests/ + hatch packages
[x] Local Design Copilot MVP: saas/ FastAPI + Streamlit + plant registry
[x] Repo purge: Lab_*.ipynb removed; examples/ are canonical demos
[x] Generalized arbitrary-motor plant + physics feasibility (workstreams A + B)
[x] Chat-first tool-calling Design Agent + grounded query_results (workstream D)
[x] Controller registry + real controllers: LQR/LQG, proper MPC, MRAC, Fuzzy (workstream C)
[x] Streamlit chat-first UI: custom motor params, always-on chat, family pick, results/export
[x] SaaS hardening: persistence, auth/multi-tenant, async runs, live events, budgets
    (workstream E / Phase E2.1–E2.6) — DONE, verified live on docker compose (Bearer key)
[ ] React/Next two-pane UI over the SSE stream (Phase E3) — NEXT
[ ] Agent eval harness extensions (workstream F) — throughout


--------------------------------------------------------------------------------
5. REVISED DETAILED BUILD SEQUENCE
--------------------------------------------------------------------------------

PHASE 0 — Plant foundation (COMPLETE)
  Artifact: examples/lab_01_plant_pid.py (open-loop section)
  Outcome: Known plant dynamics; motivation for feedback.

PHASE 1 — Closed-loop PID + metrics (COMPLETE)
  Artifact: examples/lab_01_plant_pid.py
  Outcome: Working PID baseline + deterministic metrics.

PHASE 2 — Shared Simulation & Evaluation harness (COMPLETE)
  Artifacts: dc_motor/, examples/lab_02_evaluation.py
  Outcome: Multi-scenario scorecards; JSON export for agents.
  Principle: tools COMPUTE; LLMs READ scorecards only.

---------- RESEARCH / AGENTIC ARC BEGINS HERE ----------

PHASE 3 — Spec Interpreter Agent  [PILLAR P1]  (COMPLETE)
  Artifacts:
    - dc_motor/specs.py — DesignSpec, validation/clamps, parse_spec_template
    - agents/spec_agent.py — interpret_spec / interpret_spec_llm / interpret_spec_auto
    - examples/lab_03_spec_agent.py — NL demos + wire DesignSpec into evaluate_controller
  Goal: justify LLM by translating open-ended NL specs -> formal DesignSpec.

PHASE 4 — Constraint-aware PID Tuner + Redesign Tools  [supports P3]  (COMPLETE)
  Artifacts:
    - dc_motor/failure.py — FailureDigest (failed scenarios/metrics + margins + tags)
    - dc_motor/scenarios.py — scenarios_from_spec; load torque 0.01 N·m (within ±12 V)
    - agents/pid_tuner.py — grid_search_pid / optimize_pid / tune_pid (auto)
    - examples/lab_04_pid_tuner.py — DesignSpec → tuner → scorecard + FailureDigest demos
  Goal: tools the orchestrator can CALL (not LLM inventing gains blindly).
  Methods:
    a) SciPy optimize / grid search scored by evaluate_controller
    b) Classical heuristic warm start (zn_warm_start) optional
  Outputs: candidate PIDController + scorecard + FailureDigest.
  Note: absolute settling_time < load onset (1.5 s) on load_disturbance is
  flagged POSSIBLY_INFEASIBLE_SPEC — recovery metrics come in Phase 6.

PHASE 5 — Adaptive LLM Orchestrator (core agentic loop)  [P2+P3]  (COMPLETE)
  Artifacts:
    - agents/orchestrator.py — run_design_session (script|heuristic|llm|llm_auto)
    - Action menu: tune_pid_* | expand_scenarios | relax_settling_for_load |
      call_robust/mpc/rl | identify_plant (Phase 7 stubs) | stop
    - Full action_trace (actions, scores, wall time, tokens) for ablation
    - examples/lab_05_orchestrator.py — end-to-end adaptive design session + mini A/B/C compare
  Goal: iterative redesign under uncertainty, not one-shot argmin.
  Demo result: on settling<=1.2 + load_disturbance, script fails while heuristic
  diagnoses POSSIBLY_INFEASIBLE_SPEC → relax_settling_for_load → pass.

PHASE 6 — Uncertainty digital-twin expansion  [P2]  (COMPLETE)
  Artifacts:
    - scenarios: mismatch_load, noise_low/med/high, mismatch_harsh
    - metrics.recovery_time_s + disturbance_onset_s
    - evaluate_uncertainty_batch (pass_rate, worst_case_ITAE)
    - FailureDigest tags + TAG_TO_ACTION_HINTS (incl. MODEL_DISTRUST, RECOVERY_SLOW)
    - examples/lab_06_uncertainty.py

PHASE 7 — Multi-topology specialist agents  [diagram + P2/P3]  (COMPLETE)
  Artifacts:
    - agents/specialists.py — Robust PID, MPC, AdaptivePID, plant ID (sim)
    - Same reset()/step() interface; orchestrator call_robust/mpc/rl/identify_plant live
    - examples/lab_07_specialists_certify.py (with certification)

PHASE 8 — Simulation Certification Gate + Export Package  [P4]  (COMPLETE)
  Artifacts:
    - agents/certify.py — certify_candidate / export_certified_package
    - Exports controller.json + scorecard.json + rationale.md (+ zip if ALLOW)
    - Hard BLOCK if constraints fail (no hardware path)
    - examples/lab_07_specialists_certify.py demo

PHASE 9 — Ablation & research write-up package  [P5]  (COMPLETE)
  Artifacts:
    - experiments/ablation.py — BENCHMARK_PROMPTS + run_ablation / summarize
    - examples/lab_08_ablation.py — script vs heuristic vs llm_auto compare
  Note: expand prompt suite / plant perturbations as research deepens.

--------------------------------------------------------------------------------
5B. SAAS PRODUCT FRAMING (SIMULATION-ONLY)
--------------------------------------------------------------------------------
Working product thesis:
  "Control Design Copilot" — paste plant model + NL performance specs; get a
  certified simulated controller package (parameters + scorecard + rationale)
  after an adaptive multi-method design loop under uncertainty.

Target audiences (priority order for an MVP):
  1) University labs / MOOC instructors / students  — teach feedback + AI tools
  2) Control/mechatronics engineers in SMEs         — accelerate early design
  3) Digital-twin / industrial software teams       — embed design API in PLM/CAE
  4) (Later) OEMs / robotics startups               — only after multi-plant library

Primary value propositions:
  - Time: hours of trial-and-error tuning -> minutes of guided design
  - Traceability: every claim tied to scorecard numbers + action log
  - Coverage: uncertainty scenarios by default (not one happy step plot)
  - Accessibility: NL specs for non-experts; rigorous export for experts
  - Comparability: PID vs MPC vs robust on one harness (vendor-neutral)

What makes it robust / versatile / useful (build these deliberately):
  ROBUST
    - Deterministic eval as source of truth (LLM cannot bypass)
    - Schema-validated DesignSpec; safety clamps on limits
    - Certification gate before export
    - Reproducible seeds, versioned plant/controller schemas
    - Ablation-proven when agent helps vs when script wins
  VERSATILE
    - Plant registry beyond one CTMS motor (templates: 1st-order, DC motor,
      simple servo; same Controller interface)
    - Pluggable design agents (PID/MPC/robust/RL) behind one API
    - Spec library + custom NL
    - Multi-tenant design jobs: async run, store artifacts
  USEFUL (SaaS UX)
    - Job API: POST /design {plant_id|params, nl_spec} -> job_id
    - GET /jobs/{id}: status, action_trace, scorecard, rationale, export.zip
    - UI: spec editor, live constraints preview, scenario dashboard, compare runs
    - Org features: saved plants, shared scorecards, audit log
    - Cost controls: max iterations, max tokens, cached evals

MVP SaaS slice (do not boil the ocean):
  DC motor digital twin + Spec Agent + PID tuner + LLM redesign loop +
  certification export + simple web/API. Add MPC/robust after retention is proven.

Non-goals for SaaS v1:
  - Hardware-in-the-loop, edge deploy, PLC codegen (can be v2+ narrative only)
  - Claiming "optimal control for any plant" without plant library + tests

Monetization sketches (for thesis/business case, not required to build now):
  - Education: course/lab seat license
  - Pro: per-design-job or seat + higher iteration budgets
  - Enterprise: private plant library + SSO + audit exports

Research <-> SaaS bridge:
  Research proves adaptive orchestration value (ablation).
  SaaS productizes the certified export + job API + multi-plant roadmap.

--------------------------------------------------------------------------------
6. WHAT THE LLM IS ALLOWED / FORBIDDEN TO DO
--------------------------------------------------------------------------------
ALLOWED:
  - Parse NL specs -> DesignSpec JSON
  - Read scorecards / FailureDigest
  - Choose next tool/action under a budget
  - Write design rationale citing numeric evidence
  - Propose spec clarification questions if requirements conflict

FORBIDDEN:
  - Invent metrics or claim pass/fail without evaluate_controller output
  - Bypass the simulation certification gate
  - Claim hardware readiness (out of scope)
  - Directly emit free-form controller code that skips the interface/tests
    (controllers must be constructed via tools/agents and re-evaluated)

--------------------------------------------------------------------------------
7. NON-AGENT BASELINE (ALWAYS KEEP)
--------------------------------------------------------------------------------
Ablation still compares action policies on the SAME NL specs (all specs via OpenAI):
  A) script: one-shot tune_pid_auto
  B) heuristic: hand-coded redesign rules (no LLM for actions)
  C) llm: OpenAI chooses next tool/action
There is NO regex Spec Interpreter fallback. If OpenAI is unavailable, print
llm_unavailable_message and fail clearly.

--------------------------------------------------------------------------------
8. PACKAGE / SCRIPT LAYOUT
--------------------------------------------------------------------------------
DONE (canonical Python package layout):
  dc_motor/       — plant, motor_model, state_space, feasibility, controllers, metrics,
                    scenarios, evaluate, specs, failure, registry (plants)
  agents/         — spec_agent, plant_agent, pid_tuner, orchestrator, controller_registry,
                    specialists (designers), controllers_advanced (LQR/LQG/MPC/MRAC/Fuzzy),
                    critic (grounded diagnosis), certify, design_candidate, design_agent
                    (chat-first tool-calling loop)

  Deps added for workstream C: python-control (LQR/LQG/Kalman), cvxpy + osqp
  (constrained MPC QP). MRAC + Fuzzy PID are lean hand-rolled numpy (no framework).

  Controller families behind one reset()/step() interface, registered in
  agents/controller_registry.py (kind -> designer + metadata):
    pid, robust_pid, lqr, lqg, mpc, mrac (adaptive alias), fuzzy_pid.
  Each is also an orchestrator action (call_lqr/call_lqg/call_mpc/call_mrac/
  call_fuzzy/call_robust) wired into AVAILABLE_ACTIONS + TAG_TO_ACTION_HINTS +
  heuristic/LLM menus, so auto-redesign can switch STRUCTURE (not just gains).
  RL remains roadmap-only (deferred).
  experiments/    — ablation runners, NL-spec benchmark suite (+ comparison table)
  examples/       — runnable lab_01…lab_08 demos (uv run python examples/lab_0N_*.py)
  saas/           — Design Copilot: FastAPI jobs API (+ POST /jobs/{id}/agent) + Streamlit UI
  tests/          — smoke/unit tests (mock interpret_spec; no OpenAI required)
  pyproject.toml  — hatchling packages + pytest config

Jupyter notebooks (Lab_*.ipynb) were removed; examples/ are the only demos.

NEXT (optional productization):
  Auth / multi-tenant SaaS around the local Copilot (still simulation-only)

--------------------------------------------------------------------------------
9. ENVIRONMENT CHEAT SHEET
--------------------------------------------------------------------------------
  uv sync
  copy .env.example .env   # then set OPENAI_API_KEY=...

Do NOT commit .env.
Run examples / Copilot from project root so `import dc_motor` / `import agents` work.
Examples:  uv run python examples/lab_01_plant_pid.py
Copilot:   uv run streamlit run saas/ui_streamlit.py
           uv run uvicorn saas.api:app --port 8000   # /docs = Swagger only
Tests:     uv sync --group dev && uv run pytest -q

--------------------------------------------------------------------------------
10. INSTRUCTIONS FOR A NEW CHAT AGENT
--------------------------------------------------------------------------------
- Read this file first; do not redo Phases 0–9 unless broken.
- Package layout (examples/ + tests/) and local Design Copilot MVP (saas/) are DONE.
  Repo purge (notebooks removed) is DONE. Optional next: auth/multi-tenant
  productization (still simulation-only).
- **OpenAI-only for NL/spec and LLM orchestrator actions.** No regex/template
  fallback and no silent heuristic fallback when the API key/model is missing —
  print ``llm_unavailable_message`` and raise. Ablation modes script/heuristic
  still exist as *action policies*, but they also call ``interpret_spec`` (LLM)
  for NL → DesignSpec.
- Scope is **simulation/software only** — never add hardware deploy unless the
  user explicitly reverses this lock.
- Preserve the five pillars P1–P5; do not collapse the project back into
  "LLM picks min score among fixed PIDs."
- Keep evaluate_controller as the single source of truth for metrics/pass-fail.
- New controllers: reset() / step(measurement, reference, dt).
- Always retain a non-LLM baseline path for ablation (script + heuristic modes).
- Certification gate is code-enforced; LLM may explain BLOCK/ALLOW only.
- Update this file's CURRENT STATE checkboxes when a phase completes.

================================================================================
END OF BRIEF
================================================================================
